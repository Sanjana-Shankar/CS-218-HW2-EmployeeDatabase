# Used by scripts/seed_data.py to write sample records to DynamoDB.
# The Lambda runtime already includes boto3, but keeping it here makes local
# development and seeding reproducible.
boto3>=1.34.0,<2.0.0
